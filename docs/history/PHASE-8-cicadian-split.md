# Phase 8 — Cicadian Split

**Goal:** Use the current implation of the cicadian controller for a new device type called "Curve controller" and implement a new cicadian controller that is much simpler where you don't define a custom curve, but just set temperature and brightness when it's at it warmest and it's coldest.

---

## Task 8.1 — Copy current cicadian to new device

- Copy the current implementation to a new device called "Curve controller"

## Task 8.2 — Extend new device

- Enable to set a color at specific points on the curve. So I can either chose a temperature or a color from a predefined palette of colors.

## Task 8.3 — Create new cicadian device.

- implement a new cicadian controller that is much simpler where you don't define a custom curve, but just set temperature and brightness when it's at it warmest and it's coldest.
- Graphics will be added later, so don't generate it.

# 
